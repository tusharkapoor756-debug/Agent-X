// API URL configuration
// In development, Vite proxy handles /api -> http://localhost:3000
// In production (Vercel), /api is handled by Vercel rewrites to the backend
const API_URL = '/api';

export default API_URL;
